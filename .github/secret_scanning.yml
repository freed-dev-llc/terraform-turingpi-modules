# Suppress secret scanning on documentation example code.
# README/docs frequently contain illustrative passwords/keys in example
# blocks that aren't real credentials. Real secrets are checked into
# ~/.secrets/, not the repo.
paths-ignore:
  - 'docs/**'
